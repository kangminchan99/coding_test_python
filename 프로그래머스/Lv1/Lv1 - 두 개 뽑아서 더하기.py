# from itertools import combinations

# def solution(numbers):
#   2개씩 리스트에 있는거 중복없이 다 더해서 리스트 형태로
    # combiList = list(combinations(numbers, 2))
    
    # answer = []
    
    # result = []
    
    # for i in range(len(combiList)):
    #     answer.append(combiList[i][0] + combiList[i][1])
        
    # for i in set(answer):
    #     result.append(i)
        
    # return sorted(result)
print('hiㅁㄴㅇㄴㅁㅇㄴㅁㅇ')
    